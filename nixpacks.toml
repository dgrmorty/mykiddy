# Railway (Nixpacks): Vite, Tailwind, PostCSS, TypeScript лежат в devDependencies.
# Без них сборка падает, если npm ставит только production-зависимости.
[phases.install]
cmds = ["npm ci --include=dev --prefer-offline --no-audit"]

[phases.build]
cmds = ["npm run build"]
